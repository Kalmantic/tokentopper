{
  "name": "tokentopper",
  "version": "0.2.5",
  "description": "tokentopper measures how much you use Claude Code and Codex and scores your Professional AI Usage Index. See your run-rate, tier, and verified shareable rank. Claude Code and Codex are supported today; OpenCode and other AI coding tools are on the roadmap. Private by default: prompts and code never leave your machine.",
  "type": "module",
  "bin": {
    "tokentopper": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": "^22.0.0 || ^24.0.0"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test test/*.test.ts",
    "pack:check": "npm run build && node scripts/pack-check.mjs",
    "release:assets": "node scripts/build-release-assets.mjs",
    "check": "npm run typecheck && npm test && npm run pack:check",
    "dev": "node --experimental-strip-types src/cli.ts",
    "prepublishOnly": "npm run check"
  },
  "keywords": [
    "claude",
    "claude-code",
    "codex",
    "opencode",
    "anthropic",
    "openai",
    "tokens",
    "token-usage",
    "usage",
    "cost",
    "ai",
    "llm",
    "leaderboard",
    "tokenmaxxing",
    "ccusage",
    "analytics",
    "cli",
    "openfactoryai"
  ],
  "homepage": "https://openfactoryai.com/tools/tokentopper/",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Kalmantic/tokentopper.git"
  },
  "author": "Kalmantic",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.14.0",
    "esbuild": "^0.28.1",
    "tsx": "^4.23.1",
    "typescript": "^5.5.0"
  }
}
